#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int g[1005][1005];
int time[1005][2];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, m, x; cin >> n >> m >> x;
    for(int i = 0, a, b, c; i < m; i++){
        cin >> a >> b >> c;
        
    }
    return 0;
}
